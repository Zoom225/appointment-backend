services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rdv-app
    restart: unless-stopped
    environment:
      # Local development only. Production supplies its profile and secrets at runtime.
      SPRING_PROFILES_ACTIVE: dev
      PORT: 8080
    ports:
      - "8080:8080"
